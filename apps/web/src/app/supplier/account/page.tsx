import { AccountManagementWorkspace } from "@/components/system/account-management-workspace";

export default function SupplierAccountPage() {
  return (
    <AccountManagementWorkspace
      description="Review your supplier account identity, session status, and current delivery settings."
      title="Profile"
    />
  );
}
