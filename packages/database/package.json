{
  "name": "@shop/database",
  "private": true,
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "pnpm lint && pnpm typecheck",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:migrate:local": "pnpm --filter @shop/api db:migrate:local",
    "db:studio": "drizzle-kit studio",
    "lint": "biome check src drizzle.config.ts",
    "test": "pnpm build && node dist/src/schema/index.test.js && node dist/src/schema/deliveries.test.js && node dist/src/schema/catalog-change-log.test.js && node dist/src/schema/catalog-import.test.js && node dist/src/schema/stock-initializations.test.js && node dist/src/schema/stock-movement-reasons.test.js && node dist/src/schema/stock-takes.test.js && node dist/src/schema/stock-transfer-receipts.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.0"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "drizzle-kit": "^0.31.4",
    "typescript": "^5.9.3"
  }
}
